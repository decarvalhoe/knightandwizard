// Panneau info latéral — placeholder pour extension future
// (les fonctions principales sont dans main.js pour l'instant)

export function renderInfoPanel() {
  // Stub — peut être étendu plus tard pour gérer plus de types
  const panel = document.getElementById('info-panel');
  panel.classList.remove('hidden');
}
